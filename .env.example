# ----------------------------------------------------------------------------
# File: .env.example
# Author: Vilhelm Hilding
# Project: Arxite – Hardened Web Application Architecture
# Description: Environment variable examples for local development.
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# Flask Secret Key
# ----------------------------------------------------------------------------
# Used to sign session cookies and other security-related tokens.
# In production, replace this with a long, random, unguessable string and keep it secret.
SECRET_KEY=FLASK_SECRET_KEY_HERE

# ----------------------------------------------------------------------------
# GitHub OAuth Credentials
# ----------------------------------------------------------------------------
# Client ID: Public identifier for your OAuth app. Obtain from GitHub App settings.
GITHUB_CLIENT_ID=GITHUB_CLIENT_ID_HERE
# Client Secret: Confidential credential for OAuth; do NOT commit to version control.
GITHUB_CLIENT_SECRET=GITHUB_CLIENT_SECRET_HERE
# Redirect URI: GitHub will redirect users here after authentication. Must match GitHub App config.
REDIRECT_URI=https://arxite.com/auth/callback

# ----------------------------------------------------------------------------
# Redis Configuration
# ----------------------------------------------------------------------------
# Password for Redis server used in rate limiting. Inject at runtime and avoid committing it.
REDIS_PASSWORD=REDIS_PASSWORD_HERE

# ----------------------------------------------------------------------------
# Application Logging
# ----------------------------------------------------------------------------
# Controls the verbosity of logs: DEBUG, INFO, WARNING, ERROR, CRITICAL.
LOG_LEVEL=INFO
